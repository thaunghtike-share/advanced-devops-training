# Lab 06 — lookup() Function

Use lookup to read a map value safely with a default fallback.

## Commands

```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform apply
terraform destroy
```

> Note: Storage account names must be globally unique in Azure. Labs that create storage accounts use a small random suffix.
