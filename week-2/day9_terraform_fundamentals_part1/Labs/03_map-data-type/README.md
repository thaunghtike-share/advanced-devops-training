# Lab 03 — Map Data Type

Use map values for common tags.

## Commands

```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform apply
terraform destroy
```

> Note: Storage account names must be globally unique in Azure. Labs that create storage accounts use a small random suffix.
