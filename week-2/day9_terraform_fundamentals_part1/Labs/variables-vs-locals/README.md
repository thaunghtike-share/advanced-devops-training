# Lab 07 — Variables vs Locals

Use variables for input and locals for calculated/reused values.

## Commands

```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform apply
terraform destroy
```

> Note: Storage account names must be globally unique in Azure. Labs that create storage accounts use a small random suffix.
