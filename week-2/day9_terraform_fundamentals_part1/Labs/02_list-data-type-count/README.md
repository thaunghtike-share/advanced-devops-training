# Lab 02 — List Data Type and Count

Create multiple resource groups using list + count.

## Commands

```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform apply
terraform destroy
```

> Note: Storage account names must be globally unique in Azure. Labs that create storage accounts use a small random suffix.
