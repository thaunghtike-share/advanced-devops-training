# Lab 04 — Object vs map(object)

Understand one object and multiple objects.

This lab uses outputs only, so students can focus on data structure first.

## Commands

```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform apply
terraform destroy
```

> Note: Storage account names must be globally unique in Azure. Labs that create storage accounts use a small random suffix.
